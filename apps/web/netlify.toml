[build]
  command = "pnpm install --frozen-lockfile && pnpm turbo build --filter=@dpt/web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
